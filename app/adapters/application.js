import DS from 'ember-data';

export default DS.FixtureAdapter.extend({
  // queryFixtures(fixture, query, type) {
  //   debugger;
  //   if (query.gallery_id) {
  //     return fixture.filterBy('gallery_id', parseInt(query.gallery_id));
  //   }
  // }
});
